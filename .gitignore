# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
eggs/
parts/
var/
sdist/
wheels/
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
.installed.cfg

# ── Virtual environments ──
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# ── Environment variables / secrets ──
.env
.env.local
.env.development
.env.staging
.env.production
.env.test
*.env
secrets.json
secrets.yaml
secrets.yml

# ── AWS credentials ──
.aws/
aws-credentials.json
aws_credentials.json
*.pem
*.key
*.crt
*.p12
*.pfx

# ── AWS CDK ──
cdk.out/
infrastructure/cdk/cdk.out/
infrastructure/cdk/.cdk.staging/
cdk.context.json

# ── AWS SAM ──
infrastructure/sam/.aws-sam/
.aws-sam/
samconfig.toml

# ── Node / React frontend ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# ── Frontend build outputs ──
frontend/dist/
frontend/build/
frontend/.next/
frontend/out/

# ── Lambda build artifacts (matches backend/lambdas/* structure) ──
backend/lambdas/*/package/
backend/lambdas/*/.build/
backend/lambdas/*/__pycache__/
backend/lambdas/*/.pytest_cache/
backend/lambdas/*/dist/
*.zip
layer.zip
deployment-package.zip
function.zip

# ── Bedrock / AI ──
*.model
*.bin
*.safetensors
embeddings_cache/
vector_store/
.bedrock_cache/

# ── Audio samples (never commit real 911 audio) ──
audio-samples/**/*.wav
audio-samples/**/*.mp3
audio-samples/**/*.flac
audio-samples/**/*.m4a
audio-samples/**/*.ogg
audio-samples/**/*.aac
audio-samples/*.wav
audio-samples/*.mp3

# ── Audio generation scripts ──
scripts/generate_test_calls.py
scripts/package.json
scripts/package-lock.json
scripts/node_modules/

# ── Knowledge base source documents (large files, not for git) ──
knowledge-base/**/*.pdf
knowledge-base/**/*.docx
knowledge-base/**/*.xlsx

# ── Logs ──
*.log
logs/
log/
*.log.*

# ── Testing ──
.pytest_cache/
.coverage
coverage/
htmlcov/
.tox/
.nox/
nosetests.xml
coverage.xml
*.cover
*.py,cover
junit.xml
test-results/
test_output/

# ── IDEs ──
.idea/
.vscode/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# ── macOS ──
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ── Windows ──
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# ── Jupyter ──
.ipynb_checkpoints/
*.ipynb

# ── Local config overrides ──
local_config.py
local_settings.py
config.local.json
config.local.yaml

# ── Incident data / PII (never commit real 911 data) ──
data/real/
data/incidents/
data/pii/
transcripts/
*.transcript
*.incident
caller_data/

# ── Demo seed data (production only, not for repo) ──
data/production/
seed_data/production/

# ── Claude Code ──
.claude/
